# 04 — 部署与运维

---

## 一、环境依赖

### requirements.txt

```txt
# === Web ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0
python-multipart>=0.0.9

# === LangChain ===
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# === 向量数据库 ===
qdrant-client>=1.9.0

# === 缓存 / 会话 ===
redis>=5.0.0

# === 阿里云 OSS ===
oss2>=2.18.0

# === AI 模型 ===
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.0.0
pillow>=10.0.0

# === 工具 ===
python-dotenv>=1.0.0
loguru>=0.7.0
pydantic>=2.5.0
pydantic-settings>=2.0.0

# === 测试 ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
locust>=2.27.0
```

---

## 二、配置管理 (.env)

```ini
# === Server ===
HOST=0.0.0.0
PORT=8000
WORKERS=4
LOG_LEVEL=INFO

# === Redis ===
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# === Qdrant ===
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_IMAGE=product_images
QDRANT_COLLECTION_TEXT=product_knowledge

# === 阿里云 OSS ===
OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
OSS_ACCESS_KEY_ID=your_key
OSS_ACCESS_KEY_SECRET=your_secret
OSS_BUCKET_NAME=agent-images
OSS_PREFIX=images/

# === DeepSeek ===
LLM_API_KEY=sk-deepseek-xxx
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_MAX_TOKENS=2048
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=60

# === 图片限制 ===
MAX_IMAGE_SIZE=10485760
ALLOWED_EXTENSIONS=jpg,jpeg,png,webp
```

---

## 三、Docker 部署

### Dockerfile

```dockerfile
FROM python:3.10-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动
CMD ["gunicorn", "app.main:app", \
     "-k", "uvicorn.workers.UvicornWorker", \
     "--bind", "0.0.0.0:8000", \
     "--workers", "4", \
     "--timeout", "120", \
     "--keep-alive", "5", \
     "--max-requests", "1000", \
     "--max-requests-jitter", "200", \
     "--access-logfile", "-", \
     "--error-logfile", "-"]
```

### docker-compose.yml

```yaml
version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    restart: unless-stopped
    deploy:
      replicas: 3
      resources:
        limits:
          memory: 2G

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  redis_data:
  qdrant_data:
```

---

## 四、Nginx 配置

```nginx
# nginx.conf
upstream backend_servers {
    least_conn;                               # 最少连接数负载
    server backend:8000 max_fails=3 fail_timeout=30s;
    server backend:8001 max_fails=3 fail_timeout=30s;
    server backend:8002 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    client_max_body_size 10M;

    location /api/v1/chat/stream {
        proxy_pass http://backend_servers;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_buffering off;                  # SSE 必须关缓冲
        proxy_cache off;
        chunked_transfer_encoding on;
        proxy_read_timeout 120s;
    }

    location /api/v1/upload/image {
        proxy_pass http://backend_servers;
        proxy_read_timeout 60s;
        client_body_timeout 60s;
    }

    location /api/ {
        proxy_pass http://backend_servers;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
    }

    location /health {
        proxy_pass http://backend_servers;
    }
}
```

---

## 五、限流方案

### 5.1 全局令牌桶（Redis 实现）

```python
# app/core/middleware.py
class RateLimitMiddleware:
    def __init__(self, redis_client):
        self.redis = redis_client

    async def check(self, client_ip: str) -> bool:
        key = f"ratelimit:{client_ip}"
        current = await self.redis.incr(key)
        if current == 1:
            await self.redis.expire(key, 60)
        return current <= 60  # 每分钟 60 次
```

### 5.2 限流策略

| 接口 | 限制 |
|------|------|
| `/v1/upload/image` | 30 次/分钟/IP |
| `/v1/chat` | 20 次/分钟/IP |
| `/v1/chat/stream` | 20 次/分钟/IP |
| `/v1/chat/stop` | 60 次/分钟/IP |
| `/v1/health` | 不限制 |

---

## 六、日志规范

### 6.1 日志格式（Loguru）

```
2026-05-15 10:00:00.123 | INFO     | trace_id=abc123 | POST /api/v1/chat 200 1.2s
2026-05-15 10:00:01.456 | WARNING  | trace_id=abc123 | RAG search returned empty candidates
2026-05-15 10:00:02.789 | ERROR    | trace_id=def456 | LLM timeout after 60s
```

### 6.2 全链路日志字段

```
trace_id       # 每个请求唯一追踪 ID
session_id     # 会话 ID
message_id     # 消息 ID
image_id       # 图片 ID
candidates     # top-k 商品及分数
citations      # 引用片段
timing:
  upload_ms
  embedding_ms
  vector_search_ms
  text_search_ms
  llm_first_token_ms
  llm_total_ms
  total_ms
need_clarify   # 是否需要澄清
error          # 如果有错误
```

---

## 七、健康检查

```python
# app/api/health.py
@router.get("/api/v1/health")
async def health_check():
    services = {
        "redis": await check_redis(),
        "qdrant": await check_qdrant(),
        "oss": await check_oss()
    }
    all_ok = all(s["status"] == "ok" for s in services.values())
    return {
        "status": "ok" if all_ok else "degraded",
        "timestamp": datetime.utcnow().isoformat(),
        "services": services
    }
```

Docker 健康检查配置（已在 docker-compose 中）：
```
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
  interval: 30s
  timeout: 10s
  retries: 3
```
